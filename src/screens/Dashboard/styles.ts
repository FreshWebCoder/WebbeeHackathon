import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexGrow: 1,
    padding: 8,
  },
  noItemText: {
    width: "100%",
    textAlign: "center",
    paddingVertical: 4,
    color: "#AAA"
  }
});
